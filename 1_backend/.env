
# ============================================
# ZORUNLU DEĞİŞKENLER (Bu olmadan uygulama çalışmaz)
# ============================================

# JWT Secret Key (Minimum 32 karakter - Development için)
# Güçlü bir secret oluşturmak için:
# Linux/Mac: openssl rand -base64 64
# Windows PowerShell: [Convert]::ToBase64String([System.Security.Cryptography.RandomNumberGenerator]::GetBytes(64))
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-long-for-development

# MongoDB Bağlantı URI'si
# Local MongoDB için:
MONGODB_URI=mongodb://localhost:27017/tosyam
# Docker kullanıyorsanız (docker-compose içinde):
# MONGODB_URI=mongodb://mongodb:27017/tosyam

# ============================================
# OPSİYONEL DEĞİŞKENLER (Redis ve diğer ayarlar)
# ============================================

# Redis Bağlantı URL'si
# Local Redis için (varsayılan):
REDIS_URL=redis://localhost:6379
# Docker kullanıyorsanız (docker-compose içinde):
# REDIS_URL=redis://redis:6379

# JWT Refresh Secret (Opsiyonel - belirtilmezse JWT_SECRET kullanılır)
# JWT_REFRESH_SECRET=your-refresh-secret-key-different-from-jwt-secret

# CORS Origins (virgülle ayrılmış - hangi origin'lerden istek kabul edilecek)
CORS_ORIGIN=http://localhost:3000,http://localhost:19006,http://10.0.2.2:3000

# Rate Limiting Ayarları
# THROTTLE_TTL: Kaç saniye (varsayılan: 60)
THROTTLE_TTL=60
# THROTTLE_LIMIT: TTL süresi içinde kaç istek (varsayılan: 100)
THROTTLE_LIMIT=100

# Server Portu (varsayılan: 3000)
PORT=3000

# Node Environment (development | production)
NODE_ENV=development